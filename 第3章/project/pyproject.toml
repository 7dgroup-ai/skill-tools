[project]
name = "api-test-stack"
version = "1.0.0"
description = "接口测试：自定义请求与业务参数组织（第 3 章交付工程）"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27", "PyYAML>=6.0", "jsonschema>=4.20"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
